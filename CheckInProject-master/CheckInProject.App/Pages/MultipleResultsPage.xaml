<Page x:Class="CheckInProject.App.Pages.MultipleResultsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CheckInProject.App.Pages"
      xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="MultipleResultsPage"
      DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid Background="{DynamicResource ApplicationBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Border Grid.Row="0" Background="{DynamicResource ControlFillColorDefaultBrush}" 
                Padding="20,15" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <wpfui:SymbolIcon Symbol="PeopleSearch24" FontSize="24" Margin="0,0,10,0"
                                      Foreground="{DynamicResource SystemFillColorAttentionBrush}"/>
                    <TextBlock Text="请选择您的身份" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>

                <Button Grid.Column="2" x:Name="BackButton" Click="BackButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <wpfui:SymbolIcon Symbol="Dismiss24" FontSize="16" Margin="0,0,5,0"/>
                        <TextBlock Text="取消"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- 提示信息 -->
        <Border Grid.Row="1" Background="{DynamicResource SystemFillColorAttentionBrush}" 
                CornerRadius="8" Padding="15,10" Margin="20,0,20,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <wpfui:SymbolIcon Symbol="Info24" FontSize="18" Foreground="White" Margin="0,0,10,0"/>
                <TextBlock Text="检测到多个可能匹配的人脸，请点击选择正确的身份完成签到" 
                           Foreground="White" FontSize="14" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- 候选列表 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="CandidatesPanel" Margin="20" HorizontalAlignment="Center">
                <!-- 动态生成候选卡片 -->
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</Page>
