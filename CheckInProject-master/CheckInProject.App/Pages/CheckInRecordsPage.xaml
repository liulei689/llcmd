<Page x:Class="CheckInProject.App.Pages.CheckInRecordsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CheckInProject.App.Pages"
      xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="CheckInRecordsPage"
      DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid Background="{DynamicResource ApplicationBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Border Grid.Row="0" Background="{DynamicResource ControlFillColorDefaultBrush}" 
                Padding="20,15" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <wpfui:SymbolIcon Symbol="History24" FontSize="24" Margin="0,0,10,0"
                                      Foreground="{DynamicResource SystemFillColorAttentionBrush}"/>
                    <TextBlock Text="签到记录" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- 统计信息 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Border Background="{DynamicResource SystemAccentColorBrush}" CornerRadius="15" Padding="12,5" Margin="0,0,10,0">
                        <TextBlock Text="{Binding TodayCountText}" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                    </Border>
                    <Border Background="{DynamicResource SystemFillColorSuccessBrush}" CornerRadius="15" Padding="12,5">
                        <TextBlock Text="{Binding MorningCountText}" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>

                <Button Grid.Column="2" x:Name="BackButton" Click="BackButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <wpfui:SymbolIcon Symbol="Home24" FontSize="16" Margin="0,0,5,0"/>
                        <TextBlock Text="返回首页"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- 操作栏 -->
        <Border Grid.Row="1" Background="{DynamicResource ControlFillColorDefaultBrush}" 
                Padding="20,10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" 
                           VerticalAlignment="Center" Opacity="0.7"/>

                <Button Grid.Column="1" x:Name="ExportButton" Click="ExportButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <wpfui:SymbolIcon Symbol="Save24" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="导出Excel"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- 数据表格 -->
        <Border Grid.Row="2" Background="{DynamicResource ControlFillColorDefaultBrush}" 
                CornerRadius="8" Margin="0">
            <DataGrid x:Name="RecordsDataGrid" ItemsSource="{Binding RecordsList}" 
                      AutoGenerateColumns="False" IsReadOnly="True" 
                      GridLinesVisibility="Horizontal" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="姓名" Binding="{Binding Name}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="编号" Binding="{Binding ClassID}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="10,8"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="上午" Binding="{Binding MorningTimeText}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorSuccessBrush}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="下午" Binding="{Binding AfternoonTimeText}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorSuccessBrush}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="晚上" Binding="{Binding EveningTimeText}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorSuccessBrush}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="日期" Binding="{Binding CheckInDate}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="Opacity" Value="0.7"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Height" Value="45"/>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </Border>

        <!-- 空状态 -->
        <Grid Grid.Row="2" 
              Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.4">
                <wpfui:SymbolIcon Symbol="Document24" FontSize="64" Margin="0,0,0,15"/>
                <TextBlock Text="暂无签到记录" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                <TextBlock Text="请前往" FontSize="14" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                <Button x:Name="GoToScanButton" Click="GoToScanButton_Click" Margin="0,15,0,0">
                    <StackPanel Orientation="Horizontal">
                        <wpfui:SymbolIcon Symbol="Camera24" FontSize="16" Margin="0,0,5,0"/>
                        <TextBlock Text="开始签到"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
